name: Update README and Check Links live or dead

on:
  push:
    branches:
      - main  # or your default branch name
  pull_request:
    paths:
      - 'README.md'
  schedule:
    - cron: '0 0 * * 0'  # Run weekly on Sundays at 00:00 UTC

jobs:
  update-readme-and-check-links:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.PAT }}
      
      - name: Check commit message and update README
        id: check_commit
        run: |
          LATEST_COMMIT_MSG=$(git log -1 --pretty=%B)
          if [[ $LATEST_COMMIT_MSG == *"-check"* ]]; then
            echo "CHECK_LINKS=true" >> $GITHUB_OUTPUT
            sed -i '/Commits until next check:/d' README.md
            echo -e "\nCommits until next check: 10" >> README.md
          else
            PUSH_COUNT=$(git rev-list --count HEAD)
            COMMITS_UNTIL_NEXT=$((10 - (PUSH_COUNT % 10)))
            if [ $COMMITS_UNTIL_NEXT -eq 10 ]; then
              COMMITS_UNTIL_NEXT=0
            fi
            if [ $COMMITS_UNTIL_NEXT -eq 0 ]; then
              echo "CHECK_LINKS=true" >> $GITHUB_OUTPUT
              sed -i '/Commits until next check:/d' README.md
              echo -e "\nCommits until next check: 10" >> README.md
            else
              echo "CHECK_LINKS=false" >> $GITHUB_OUTPUT
              sed -i '/Commits until next check:/d' README.md
              echo -e "\nCommits until next check: $COMMITS_UNTIL_NEXT" >> README.md
            fi
          fi

      - name: Commit README changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add README.md
          git diff --quiet && git diff --staged --quiet || (git commit -m "Update README counter [skip ci]" && git push)

      - name: Check links with lychee
        id: lychee
        uses: lycheeverse/lychee-action@v1
        with:
          args: --verbose --no-progress './README.md'
          output: ./lychee-out.md
          format: markdown
          fail: false
        env:
          GITHUB_TOKEN: ${{ secrets.PAT }}

      - name: Debug lychee output
        run: |
          echo "Lychee exit code: ${{ steps.lychee.outputs.exit_code }}"
          cat lychee-out.md

      - name: Remove broken links and descriptions
        if: ${{ contains(steps.lychee.outputs.result, '🚫 Errors') }}
        run: |
          echo "Removing broken links..."
          grep -oP '(?<=\[).*?(?=\])' lychee-out.md | while read -r line; do
            echo "Removing line containing: $line"
            sed -i "/\[$line\]/d" README.md
          done
          echo "Broken links removed."

      - name: Create Pull Request for removed links
        if: ${{ contains(steps.lychee.outputs.result, '🚫 Errors') }}
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.PAT }}
          commit-message: Remove broken links from README
          title: Remove broken links from README
          body: |
            This PR removes broken links and their descriptions from the README.md file.
            Please review the changes and merge if appropriate.
          branch: remove-broken-links
          base: main

      - name: Debug pull request creation
        if: ${{ contains(steps.lychee.outputs.result, '🚫 Errors') }}
        run: |
          echo "Pull request creation attempted."
          git status
          git diff

      - name: Update README after successful link check
        if: steps.check_commit.outputs.CHECK_LINKS == 'true' && success()
        run: |
          sed -i '/Last checked on:/d' README.md
          echo -e "\nLast checked on: $(date +'%Y-%m-%d')" >> README.md
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add README.md
          git diff --quiet && git diff --staged --quiet || (git commit -m "Update link check date in README [skip ci]" && git push)